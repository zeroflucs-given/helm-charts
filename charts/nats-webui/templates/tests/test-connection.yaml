apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nats-webui.fullname" . }}-test-connection"
  labels:
    {{- include "nats-webui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nats-webui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
